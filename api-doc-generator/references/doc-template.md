# {接口业务名} 接口文档

> 复制本模板后，将 `{}` 占位符替换为实际内容，并删除本提示行。

## 接口概述

{一句话定位接口用途}，{1-2 句描述适用业务场景}。

---

## 基础信息

| 项 | 值 |
|----|----|
| Method | `GET` / `POST` |
| Content-Type | `application/json` 或 N/A（GET 请求无 body） |
| 字符编码 | UTF-8 |

### 多环境 URL

| 环境 | URL |
|------|-----|
| qa（测试） | `http://flightadminapi.qa.17usoft.com/gdsaccmng{path}` |
| uat（用户验收） | `http://flightadminapi.uat.17usoft.com/gdsaccmng{path}` |
| pre（预发） | `http://flightadminapi.t.17usoft.com/gdsaccmng{path}` |
| prd（生产） | `http://flightadminapi.17usoft.com/gdsaccmng{path}` |

> 将 `{path}` 替换为实际接口路径。

---

## 入参说明

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| `{参数名}` | Path / Query / Body | `{类型}` | 是/否 | {含义；枚举值；长度/范围} |

### 嵌套入参说明（如有）

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `{parent}.{child}` | `{类型}` | 是/否 | {含义} |
| `{parent}[].{child}` | `{类型}` | 是/否 | {含义} |

条件必填字段示例：

| 参数名 | 必填条件 |
|--------|----------|
| `{参数名}` | 条件必填：当 `{另一个参数}` 为 `{某值}` 时必填 |

---

## 返参说明

### 外层统一响应

| 字段 | 类型 | 必返回 | 说明 |
|------|------|--------|------|
| `success` | `boolean` | 是 | 业务是否成功 |
| `errorCode` | `String` | 失败时返回 | 错误码 |
| `errorMessage` | `String` | 失败时返回 | 错误描述 |
| `data` | `{类型}` | 成功时返回 | 业务数据 |

### data 字段结构

| 字段 | 类型 | 说明 |
|------|------|------|
| `{字段名}` | `{类型}` | {含义} |
| `{字段名}[].{子字段}` | `{类型}` | {含义} |

### 枚举值说明（如有）

| 字段 | 取值 | 含义 |
|------|------|------|
| `{字段}` | `{value}` | {含义} |

---

### 入参示例

```json
{
  "field1": "value1",
  "field2": 100
}
```

### 返参示例

成功：

```json
{
  "success": true,
  "errorCode": null,
  "errorMessage": null,
  "data": {
    "id": 123,
    "name": "示例"
  }
}
```

失败：

```json
{
  "success": false,
  "errorCode": "1000",
  "errorMessage": "参数错误：field1 不能为空",
  "data": null
}
```

---

## 场景/分支行为（如有多个模式）

| 场景 | 触发条件 | 行为差异 |
|------|----------|----------|
| 场景A | `{参数}={某值}` | {描述} |
| 场景B | `{参数}={另一值}` | {描述} |

---

## 异常/边界行为

| 异常场景 | 触发条件 | 处理方式 | 调用方应对 |
|----------|----------|----------|------------|
| 参数缺失 | 必填参数为空 | 返回 `errorCode=1000` | 校验后重试 |
| 数据不存在 | 查询结果为空 | 返回 `data=null` | 按空数据处理 |
| 权限不足 | 用户无权限 | 返回 `errorCode=2003` | 联系管理员 |

---

## 错误码

| errorCode | 含义 | 触发条件 | 调用方应对 |
|-----------|------|---------|------------|
| `0000` | 成功 | 业务正常 | 处理 `data` |
| `1000` | 参数错误 | 入参缺失或非法 | 校验后重试 |
| `{业务错误码}` | {含义} | {触发条件} | {应对方式} |

> 通用错误码（`0000`/`1000`/`2000`/`9999`）适用于所有接口，此处仅列出本接口额外触发的业务错误码。

---

## 代码位置

| 项 | 值 |
|----|----|
| Controller | `{ControllerClassName}` |
| 方法 | `{methodName}` |
| 文件路径 | `{src/main/java/.../Controller.java}` |
| 请求 DTO | `{RequestDTO}` |
| 响应 DTO | `{ResponseDTO}` |
